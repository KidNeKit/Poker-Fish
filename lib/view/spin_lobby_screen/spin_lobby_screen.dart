import 'package:flutter/material.dart';

class SpinLobbyScreen extends StatelessWidget {
  const SpinLobbyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Spin&Go'),
    );
  }
}
